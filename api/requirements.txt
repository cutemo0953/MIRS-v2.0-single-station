# MIRS Backend Dependencies for Vercel
# Medical Inventory Resilience System v2.1

fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0
qrcode>=7.4.2
Pillow>=10.1.0
PyJWT>=2.8.0
httpx>=0.25.0  # CIRS Hub proxy (Hub-Satellite Architecture)
pytz>=2024.1   # Timezone handling for blood bank expiry

# PostgreSQL support (Neon)
psycopg2-binary>=2.9.9

# v2.1: Anesthesia PDF Preview (HTML only on Vercel)
jinja2>=3.1.0  # HTML template rendering for PDF preview
# Note: weasyprint/matplotlib NOT included (require system deps)
