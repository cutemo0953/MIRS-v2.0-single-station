# Blood Bank PWA DEV SPEC v2.7

**版本**: 2.7
**日期**: 2026-01-13
**狀態**: MIRS Legacy Style UI 重構完成
**基於**: v2.6 + UI Visual Refinement 需求

---

## v2.6 → v2.7 變更摘要 (MIRS Legacy Style)

### 核心目標

> **「當使用者打開 App，應該立刻看到『我有沒有 O- 血？』而不需要閱讀列表」**

v2.6 功能正確，但視覺設計失去了原始 MIRS Dashboard 的「優雅與即時性」。v2.7 重構 UI 以匹配 **MIRS Legacy Style**。

### UI 改進項目

| 項目 | 狀態 | 說明 |
|------|------|------|
| **Big Colored Cards Grid** | ✅ 完成 | 2x4 血型卡片網格，非空深紅/空灰色 |
| **Click-to-Filter** | ✅ 完成 | 點擊卡片過濾下方列表 |
| **Left Border Color Coding** | ✅ 完成 | 血袋清單依血品類型顯示左側 8px 色條 |
| **Prominent Unit ID & Expiry** | ✅ 完成 | 血袋 ID 和效期放大突顯 |
| **Emergency FAB** | ✅ 完成 | 浮動緊急領血按鈕 (Panic Button) |
| **Blinking Animation** | ✅ 完成 | FAB 慢速脈動動畫 |

---

## 1. Top Dashboard - Big Colored Cards Grid (The Soul of MIRS)

### 設計規格

```
┌─────────────────────────────────────────────────────────────────┐
│                    MIRS Legacy Style Dashboard                    │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │   O+     │ │   O-     │ │   A+     │ │   A-     │           │
│  │          │ │          │ │          │ │          │           │
│  │  12 袋   │ │   3 袋   │ │   8 袋   │ │   2 袋   │           │
│  │          │ │  ⚠ 1即期 │ │          │ │          │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │   B+     │ │   B-     │ │  AB+     │ │  AB-     │           │
│  │          │ │          │ │          │ │          │           │
│  │   5 袋   │ │   0 袋   │ │   4 袋   │ │   0 袋   │           │
│  │          │ │  (灰色)  │ │          │ │  (灰色)  │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│                                                                   │
│  總庫存: 34 袋                    [全血][紅血球][血漿][血小板]    │
└─────────────────────────────────────────────────────────────────┘
```

### 樣式規則

| 狀態 | 背景色 | 數量顏色 | 說明 |
|------|--------|----------|------|
| 有庫存 | `bg-red-900` | `text-yellow-300` | 深紅背景，黃色數字高對比 |
| 零庫存 | `bg-gray-700` | `text-gray-500` | 灰色背景，弱化顯示 |
| 即將過期 | - | `text-orange-400 animate-pulse` | 橙色脈動警告 |

### 互動行為

```javascript
// 點擊卡片過濾
filterByBloodType(bloodType) {
    if (this.dashboardFilter === bloodType) {
        this.dashboardFilter = '';  // 再次點擊清除
    } else {
        this.dashboardFilter = bloodType;
    }
}
```

### HTML 實作

```html
<div class="grid grid-cols-4 gap-3">
    <template x-for="blood in groupedByBloodType" :key="blood.blood_type">
        <div @click="filterByBloodType(blood.blood_type)"
             class="cursor-pointer rounded-xl p-4 shadow-lg transform hover:scale-105"
             :class="blood.total > 0 ? 'bg-red-900' : 'bg-gray-700'">
            <!-- Blood Type - HUGE -->
            <div class="text-3xl sm:text-4xl font-black text-white" x-text="blood.blood_type"></div>
            <!-- Count - Yellow -->
            <div class="text-xl font-bold"
                 :class="blood.total > 0 ? 'text-yellow-300' : 'text-gray-500'">
                <span x-text="blood.total"></span> 袋
            </div>
            <!-- Expiring Indicator -->
            <div x-show="blood.expiring > 0" class="text-orange-400 animate-pulse">
                ⚠ <span x-text="blood.expiring"></span> 即期
            </div>
        </div>
    </template>
</div>
```

---

## 2. Unit List - Clean & Scannable

### 設計規格

移除多餘的邊框和灰色背景，改用**左側 8px 色條**依血品類型區分：

```
┌─────────────────────────────────────────────────────────────────┐
│ ▌▌▌▌  O+   [全血]   [可用]                      [FIFO 優先]    │
│ ▌▌▌▌  ───────────────────────────────────────────────────────  │
│ ▌▌▌▌  BU-1705123456-abcd1234              效期: 48h            │
│ ▌▌▌▌  ───────────────────────────────────────────────────────  │
│ 深紅   [      發血      ]  [     預約     ]                     │
└─────────────────────────────────────────────────────────────────┘
```

### 左側邊框顏色規則

| 血品類型 | 邊框顏色 | Tailwind Class |
|----------|----------|----------------|
| WB (全血) | 深紅 | `border-l-8 border-red-900` |
| PRBC (紅血球) | 紅 | `border-l-8 border-red-600` |
| FFP (血漿) | 黃 | `border-l-8 border-yellow-400` |
| PLT (血小板) | 淺黃 | `border-l-8 border-yellow-200` |
| CRYO (冷沉澱) | 青 | `border-l-8 border-cyan-400` |

### 突顯元素

1. **Unit ID**: `font-mono font-bold text-gray-700` - 等寬粗體
2. **Expiry**: 依剩餘時間變色
   - `> 72h`: `text-green-600`
   - `24-72h`: `text-orange-600`
   - `< 24h`: `text-red-600 animate-pulse`

### JavaScript 實作

```javascript
getUnitTypeBorderColorClass(unitType) {
    const classes = {
        'WB': 'border-red-900',
        'PRBC': 'border-red-600',
        'FFP': 'border-yellow-400',
        'PLT': 'border-yellow-200',
        'CRYO': 'border-cyan-400'
    };
    return classes[unitType] || 'border-gray-300';
}
```

---

## 3. Emergency FAB - The Panic Button

### 設計規格

浮動在畫面右下角的緊急領血按鈕：

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                   │
│                              主畫面內容                           │
│                                                                   │
│                                                                   │
│                                         ┌───────────────────┐    │
│                                         │  🚨 緊急領血       │    │
│                                         │  (慢速脈動)        │    │
│                                         └───────────────────┘    │
│  [Tab 導覽列]                                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 樣式規則

| 元素 | 樣式 |
|------|------|
| 按鈕背景 | `bg-red-600 hover:bg-red-700` |
| 邊框 | `border-4 border-red-300` |
| 動畫 | `animate-pulse-slow` (2s 週期) |
| 定位 | `fixed bottom-20 right-4 z-40` |

### 動畫定義

```css
@keyframes pulse-slow {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.9; transform: scale(1.05); }
}
.animate-pulse-slow {
    animation: pulse-slow 2s ease-in-out infinite;
}
```

### 點擊後 Modal

```
┌─────────────────────────────────────────────────────────────────┐
│                    🚨 緊急領血 Emergency Release                  │
│                    ─────────────────────────────                  │
│                    僅限 O 型血 (萬能供血)                         │
│                                                                   │
│    緊急發血將跳過正常流程，事後需補開醫囑                         │
│                                                                   │
│    ┌─────────────────────────────────────────────────────────┐  │
│    │                     O+                                   │  │
│    │                   緊急發血                               │  │
│    └─────────────────────────────────────────────────────────┘  │
│                                                                   │
│    ┌─────────────────────────────────────────────────────────┐  │
│    │                     O-          [推薦]                   │  │
│    │                   萬能血型                               │  │
│    └─────────────────────────────────────────────────────────┘  │
│                                                                   │
│    ┌─────────────────────────────────────────────────────────┐  │
│    │                     取消                                 │  │
│    └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 原始 MIRS vs Blood Bank PWA v2.7 比較

### Dashboard 比較

| 項目 | 原始 MIRS | Blood Bank PWA v2.7 |
|------|-----------|---------------------|
| **卡片佈局** | 8 欄 (桌面) / 4 欄 (手機) | 4 欄固定 (手機優化) |
| **背景色** | 深紅漸層 | 深紅 (非空) / 灰色 (空) |
| **數字顏色** | 白色 | 黃色 (更高對比) |
| **即期警示** | ❌ 無 | ✅ 橙色脈動 |
| **點擊過濾** | ❌ 無 | ✅ 有 |

### 列表比較

| 項目 | 原始 MIRS | Blood Bank PWA v2.7 |
|------|-----------|---------------------|
| **顯示方式** | 無獨立列表 | 獨立 Tab + Unit-level |
| **狀態區分** | ❌ 無 | ✅ 左側色條 (依血品類型) |
| **Unit ID** | ❌ 無 | ✅ 等寬粗體突顯 |
| **效期顯示** | ❌ 無 | ✅ 依剩餘時間變色 |

### 緊急功能比較

| 項目 | 原始 MIRS | Blood Bank PWA v2.7 |
|------|-----------|---------------------|
| **緊急入口** | 表單內按鈕 | FAB 浮動按鈕 (永遠可見) |
| **視覺提示** | ❌ 無 | ✅ 慢速脈動 |
| **一鍵發血** | ❌ 需填表 | ✅ O+/O- 直接點擊 |

---

## 技術實作細節

### 新增 JavaScript

```javascript
// v2.7: Dashboard filter state
dashboardFilter: '',

// v2.7: 總庫存
get totalUnits() {
    return this.groupedByBloodType.reduce((sum, b) => sum + b.total, 0);
},

// v2.7: 總即期
get totalExpiring() {
    return this.groupedByBloodType.reduce((sum, b) => sum + b.expiring, 0);
},

// v2.7: Dashboard 過濾後的 availability
get filteredByDashboard() {
    if (!this.dashboardFilter) return this.availability;
    return this.availability.filter(a => a.blood_type === this.dashboardFilter);
},

// v2.7: 點擊血型卡片過濾
filterByBloodType(bloodType) {
    if (this.dashboardFilter === bloodType) {
        this.dashboardFilter = '';
    } else {
        this.dashboardFilter = bloodType;
    }
},

// v2.7: 血品類型左側邊框顏色
getUnitTypeBorderColorClass(unitType) {
    const classes = {
        'WB': 'border-red-900',
        'PRBC': 'border-red-600',
        'FFP': 'border-yellow-400',
        'PLT': 'border-yellow-200',
        'CRYO': 'border-cyan-400'
    };
    return classes[unitType] || 'border-gray-300';
}
```

### 檔案變更

| 檔案 | 變更 |
|------|------|
| `frontend/blood/index.html` | Big Cards Grid + Unit List + FAB |
| `frontend/blood/service-worker.js` | v2.7.0 快取刷新 |

---

## 版本歷史

| 版本 | 日期 | 變更 |
|------|------|------|
| v2.5 | 2026-01-12 | P4: 待補單追蹤 + 預約逾時自動釋放 |
| v2.6 | 2026-01-13 | UI 改進: 仿原始 MIRS 血庫設計、血品色塊、WB 支援 |
| v2.7 | 2026-01-13 | MIRS Legacy Style: Big Cards Grid、左側色條、Emergency FAB |

---

**文件完成**
**撰寫者**: Claude Code
**日期**: 2026-01-13
